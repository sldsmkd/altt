upstream application {
  server <%= node[:webs][0] %>;
  server <%= node[:webs][1] %>;
}

server {
  access_log  /var/log/nginx/localhost.access.log;
  listen 80;
  location / {
    proxy_pass http://application;
  }
}
